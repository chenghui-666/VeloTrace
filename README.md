# VeloTrace Reconciles Divergent Velocity and Trajectory in Single-cell Transcriptomics with Deep Neural ODE
VeloTrace is a Python package for RNA velocity trajectory inference

## Installation
Conda（推荐）
```bash
GPU：conda env create -f environment.yml
CPU：删除 environment.yml 中 nvidia 条目后再创建
```

pip：
```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
```

## Tutorial
Jupyter notebooks showing how to reproduce the main figure panels, along with all required processed data files. These can be found under the xxxx folder in this repository or on our ReadTheDocs page.

data xxxxxx

tutorial xxxxx
